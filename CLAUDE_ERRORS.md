# Errores Conocidos — derup

Registro de errores y lecciones aprendidas para evitar repetirlos.

| Fecha | Área | Error | Causa | Fix | Regla |
|-------|------|-------|-------|-----|-------|
| 2026-03-24 | OpenClaw/VPS | Asumió lint clean == build clean | Pre-existing lint violations en Canvas.tsx y ContextMenu.tsx no relacionadas al cambio actual | Usar `npm run build` como check autoritativo de TS | `config` — build es el check correcto, lint tiene violaciones conocidas que preexisten |
| 2026-03-24 | OpenClaw/VPS | Recarga de gateway falló por PID desactualizado | openclaw-gateway no tiene systemd, el PID deriva entre reinicios | `ps aux \| grep openclaw-gateway` antes de `kill -HUP <pid>` | `config` — siempre buscar el PID en caliente antes de enviar señal |
| 2026-03-24 | AI Command Protocol | Cast innecesario en `updateNode` con atributos parciales | Se usó cast explícito cuando `Partial<AttributeNode>` ya es asignable a `Partial<ERNode>` | Usar `Partial<AttributeNode>` directamente sin cast | `logic` — evitar casts cuando la jerarquía de tipos ya lo permite |
| 2026-03-24 | AI Command Protocol | `useCallback` agregado a helpers que cierran sobre estado React | Los helpers cierran sobre `nodes`/`connections` — useCallback no aporta y complica | Usar `const` plano dentro del cuerpo del componente | `logic` — no wrappear con useCallback helpers que dependen de state directo |
| 2026-03-24 | AI Command Protocol | Definición duplicada de `findEntityByLabel` en App.tsx | Hay DOS definiciones locales (~líneas 3080 y 3180) dentro de bloques `if` además de la de componente (~2336) | Nuevos helpers al nivel de componente junto a la definición en línea 2336 | `logic` — verificar duplicados de helpers locales antes de agregar nuevos |
| 2026-03-24 | WebSocket rooms | TS6133 por variables de estado declaradas pero no leídas en JSX | `roomPeers` actualizado pero nunca consumido en render — TS strict mode lo rechaza | Eliminar el estado o usarlo en JSX | `syntax` — no declarar state que no se lee; TS strict lo rompe |
| 2026-03-24 | WebSocket rooms | `replace_all` dejó una ocurrencia sin reemplazar por indentación diferente | La herramienta reemplaza texto exacto — una instancia tenía contexto indentado diferente | Grep post-replace para confirmar cero instancias restantes | `logic` — siempre grep después de replace_all para verificar completitud |
| 2026-03-24 | chatParser | Test falló por `startsWith` antes de Levenshtein en fuzzy-match | "debilidad".startsWith("debil") → siempre dispara `hasWeakKeyword`; palabras más largas matchean prefijos cortos | Testear con `node --input-type=module` antes de escribir assertions | `logic` — no asumir output del parser por la spec; verificar ejecución real |
| 2026-03-24 | chatParser | `extractAttributesForExistingEntity` no stripea prefijos de contexto | El extractor devuelve artefactos como `["de Cliente a nombre", "email"]` sin limpiar el frente | Assertar los ítems trailing limpios, no el primer artefacto | `logic` — leer el código del extractor antes de escribir assertions de forma |
| 2026-03-24 | chatParser | `clear-diagram` requiere "todo" explícito en el texto | "reset" solo y "borrar el diagrama completo" sin "todo" devuelven null — la condición es deleteIntent AND "todo" | Incluir "todo" en los casos de test de clear | `logic` — verificar condiciones booleanas compuestas en el parser antes de testear |
| 2026-03-24 | Documentación | Write falló por guard "file not read yet" en archivos existentes | La herramienta requiere Read previo incluso en rewrites totales | Siempre Read antes de Write, incluso para sobreescritura completa | `config` — Read obligatorio antes de Write aunque el contenido se descarte |
| 2026-03-24 | Documentación | Asumió que archivo no existía porque no estaba en git log reciente | `docs/` ya tenía ARCHITECTURE/CONTRIBUTING/DEPLOY_GCP — git log no es fuente exhaustiva | `ls` en el directorio antes de asumir que un archivo es net-new | `config` — verificar existencia con ls antes de Write directo |
| 2026-03-24 | Unit tests hooks | `isValidConnection` no incluye pares `relationship-isa` según spec pero sí en código real | La descripción de la spec estaba incorrecta respecto a los validPairs reales | Leer el código de Canvas.tsx directamente para verificar validPairs | `logic` — no confiar en comentarios de spec para reglas de validación; leer el código |
| 2026-03-24 | Unit tests hooks | `renderHook` + `useEffect` no flusheó inicialización antes de assertion | Faltaba `await act(async () => {})` post-renderHook para aplicar el efecto de deserialización | Agregar `await act(async () => {})` tras `renderHook()` antes de assertar estado | `logic` — siempre flushear efectos con act() antes de assertar state en renderHook |
| 2026-03-24 | Unit tests hooks | Fake timers leakearon a archivos de test no relacionados | `vi.useFakeTimers()` a nivel de módulo en vez de beforeEach/afterEach | Mover a `beforeEach`/`afterEach` con `vi.useRealTimers()` en teardown | `config` — fake timers siempre en beforeEach/afterEach, nunca a nivel de módulo |
| 2026-03-24 | Canvas rendering | Importar Canvas React component para testear funciones matemáticas puras | El componente tiene side effects y deps de React que rompen el entorno de test | Copiar las funciones puras verbatim como helpers locales en el test file | `logic` — no importar componentes React para testear funciones puras; aislarlas |
